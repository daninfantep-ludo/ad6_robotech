        <div style="position: relative; flex: 0 0 90px; margin-right: 3px;">
            <img class="profile-img" 
                src="{{actor.img}}" 
                title="{{actor.name}} {{ localize "Ad6.Mensajes.clickCambiar"}}" 
                data-actor-id="{{actor.id}}"
                data-action="editPortrait"
                width="90" height="90" 
                style="display: block; border: 1px solid var(--color-brillo) "/>
                </div>